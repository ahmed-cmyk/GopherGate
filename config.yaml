service-name: "API Gateway"

server:
  port: 8080

routes:
  - path: /api/users
    target: "http://user-service:8001"
    methods: ["GET", "POST"]
    strip-prefix: true
    middlewares:
      - rate_limit
      - logging